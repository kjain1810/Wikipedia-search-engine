word collection, each doc will be of type:
{
    'id': ID,
    'word': str,
    'docs': [
        {
            'docID': ID,
            'frequency': int,
            'title': bool,
            'category': bool,
            'references': bool,
            'links': int
        }
    ]
}

doc collection, each doc will be of type:
{
    'id': ID,
    'title': str,
    'tokens': int
}